import { readFile, writeFile } from 'node:fs/promises';

async function read(path) {
  return readFile(path, 'utf8');
}

async function write(path, content) {
  await writeFile(path, content, 'utf8');
}

function replaceOnce(source, before, after, label) {
  if (!source.includes(before)) throw new Error(`Missing ${label}`);
  return source.replace(before, after);
}

function replaceTailFrom(source, marker, replacement, label) {
  const index = source.indexOf(marker);
  if (index < 0) throw new Error(`Missing ${label}`);
  return source.slice(0, index) + replacement;
}

let admin = await read('public/admin.js');

const stateBlock = `const state = {\n  bootstrap: null,\n  image: null,\n  events: [],\n  edit: null,\n};\n`;
const destinationState = `${stateBlock}\nconst DESTINATION_TTL_MS = 15_000;\nconst destinationState = {\n  monitors: null,\n  loadedAt: 0,\n  loading: null,\n  preferredMonitorId: null,\n};\n`;
if (!admin.includes('const DESTINATION_TTL_MS = 15_000;')) {
  admin = replaceOnce(admin, stateBlock, destinationState, 'admin destination state insertion point');
}

const monitorStart = admin.indexOf('function updateMonitorOptions() {');
const monitorEnd = admin.indexOf('function updateType(type) {', monitorStart);
if (monitorStart < 0 || monitorEnd < 0) throw new Error('Missing monitor owner block');
const monitorOwner = `function monitorMatchesType(monitor, type) {\n  if (monitor.canManage !== true) return false;\n  return type === 'giveaway'\n    ? monitor.triggerKeyword === 'ラキショ'\n    : monitor.triggerKeyword !== 'ラキショ';\n}\n\nfunction monitorLabel(monitor, duplicateChannelIds) {\n  const base = \`#\${monitor.channelName}\`;\n  if (!duplicateChannelIds.has(String(monitor.channelId))) return base;\n  const calendar = String(monitor.calendarName || monitor.calendarId || '').trim();\n  return calendar ? \`\${base} (\${calendar})\` : \`\${base} (設定\${monitor.id})\`;\n}\n\nfunction updateMonitorOptions(preferredMonitorId = null) {\n  const type = selectedType();\n  const monitorSelect = $('#monitor');\n  const previous = String(preferredMonitorId || destinationState.preferredMonitorId || monitorSelect.value || '');\n  const source = Array.isArray(destinationState.monitors)\n    ? destinationState.monitors\n    : (state.bootstrap?.monitors || []);\n  const filtered = source.filter(monitor => monitorMatchesType(monitor, type));\n  const channelCounts = new Map();\n  for (const monitor of filtered) {\n    const key = String(monitor.channelId);\n    channelCounts.set(key, (channelCounts.get(key) || 0) + 1);\n  }\n  const duplicateChannelIds = new Set(\n    [...channelCounts].filter(([, count]) => count > 1).map(([channelId]) => channelId),\n  );\n\n  monitorSelect.replaceChildren();\n  for (const monitor of filtered) {\n    const option = document.createElement('option');\n    option.value = String(monitor.id);\n    option.textContent = monitorLabel(monitor, duplicateChannelIds);\n    monitorSelect.append(option);\n  }\n\n  const matching = [...monitorSelect.options].find(option => option.value === previous);\n  if (matching) monitorSelect.value = matching.value;\n\n  if (filtered.length === 0) {\n    const option = document.createElement('option');\n    option.value = '';\n    option.textContent = type === 'giveaway'\n      ? '抽選用の投稿先がありません'\n      : '通常投稿用の投稿先がありません';\n    monitorSelect.append(option);\n  }\n}\n\nasync function refreshMonitorOptions({ force = false, preferredMonitorId = null, silent = false } = {}) {\n  if (preferredMonitorId) destinationState.preferredMonitorId = String(preferredMonitorId);\n\n  const fresh = destinationState.monitors\n    && (Date.now() - destinationState.loadedAt) < DESTINATION_TTL_MS;\n  if (!force && fresh) {\n    updateMonitorOptions(preferredMonitorId);\n    return destinationState.monitors;\n  }\n\n  if (destinationState.loading) {\n    try {\n      await destinationState.loading;\n      updateMonitorOptions(preferredMonitorId);\n      return destinationState.monitors;\n    } catch {\n      return destinationState.monitors;\n    }\n  }\n\n  const mode = force ? 'refresh' : '1';\n  destinationState.loading = api(\`/api/admin/bootstrap?channels=\${encodeURIComponent(mode)}\`)\n    .then(result => {\n      destinationState.monitors = Array.isArray(result.monitors) ? result.monitors : [];\n      destinationState.loadedAt = Date.now();\n      state.bootstrap.monitors = destinationState.monitors;\n      updateMonitorOptions(preferredMonitorId);\n      return destinationState.monitors;\n    })\n    .catch(error => {\n      if (silent) return destinationState.monitors;\n      showNotice(\`投稿先を更新できませんでした。\${error.message ? \` \${error.message}\` : ''}\`, true);\n      throw error;\n    })\n    .finally(() => {\n      destinationState.loading = null;\n    });\n\n  return destinationState.loading;\n}\n\n`;
admin = admin.slice(0, monitorStart) + monitorOwner + admin.slice(monitorEnd);

const oldEditListener = `document.addEventListener('reactus:edit-event', event => {\n  const source = event.detail;\n  if (!source?.calendarId || !source?.id) return;\n  void editEvent(source);\n});\n`;
const newEditListener = `document.addEventListener('reactus:edit-event', event => {\n  const source = event.detail;\n  if (!source?.calendarId || !source?.id) return;\n  if (source.monitorId) {\n    destinationState.preferredMonitorId = String(source.monitorId);\n    void refreshMonitorOptions({\n      force: true,\n      preferredMonitorId: source.monitorId,\n      silent: true,\n    });\n  }\n  void editEvent(source);\n});\n`;
admin = replaceOnce(admin, oldEditListener, newEditListener, 'edit-event monitor refresh');

const oldInitTail = `  $('#app').classList.remove('hidden');\n  $('#identity').textContent = \`\${state.bootstrap.guild.name} · \${state.bootstrap.user.displayName}\`;\n  populateRoles();\n  editControls();\n  resetCreateMode();\n  await loadEvents();\n}\n\n$$('.segment').forEach(button => button.addEventListener('click', () => {\n  if (!state.edit) updateType(button.dataset.type);\n}));\n`;
const newInitTail = `  destinationState.monitors = Array.isArray(state.bootstrap.monitors) ? state.bootstrap.monitors : [];\n  destinationState.loadedAt = 0;\n\n  $('#app').classList.remove('hidden');\n  $('#identity').textContent = \`\${state.bootstrap.guild.name} · \${state.bootstrap.user.displayName}\`;\n  populateRoles();\n  editControls();\n  resetCreateMode();\n  void refreshMonitorOptions({ silent: true });\n  await loadEvents();\n}\n\n$$('.segment').forEach(button => button.addEventListener('click', () => {\n  if (state.edit) return;\n  destinationState.preferredMonitorId = null;\n  updateType(button.dataset.type);\n  void refreshMonitorOptions({ force: true, silent: true });\n}));\n$('#monitor').addEventListener('focus', () => {\n  void refreshMonitorOptions({\n    force: true,\n    preferredMonitorId: $('#monitor').value,\n    silent: true,\n  });\n});\n$('#monitor').addEventListener('change', () => {\n  destinationState.preferredMonitorId = $('#monitor').value || null;\n});\n`;
admin = replaceOnce(admin, oldInitTail, newInitTail, 'admin initialize monitor lifecycle');
await write('public/admin.js', admin);

let futureScope = await read('public/common/admin-future-scope.js');
futureScope = futureScope.replace("import './admin-post-destinations.js';\n", '');
await write('public/common/admin-future-scope.js', futureScope);

const dashboardPolish = `import assert from 'node:assert/strict';\nimport { readFile } from 'node:fs/promises';\nimport test from 'node:test';\n\nconst entryPath = new URL('../public/common/admin-future-scope.js', import.meta.url);\nconst guidePath = new URL('../public/common/admin-japanese-ui.js', import.meta.url);\nconst adminPath = new URL('../public/admin.js', import.meta.url);\nconst toolsPath = new URL('../public/common/admin-tools.js', import.meta.url);\nconst calendarSettingsPath = new URL('../public/common/admin-calendar-settings.js', import.meta.url);\nconst settingsPath = new URL('../public/common/admin-calendar-settings-usability.js', import.meta.url);\nconst navigationPath = new URL('../public/common/admin-navigation-polish.js', import.meta.url);\nconst announcementsPath = new URL('../public/common/admin-announcements.js', import.meta.url);\nconst retiredPermissionsPath = new URL('../public/common/admin-manageable-targets.js', import.meta.url);\nconst retiredLabelsPath = new URL('../public/common/admin-monitor-labels.js', import.meta.url);\nconst retiredDestinationsPath = new URL('../public/common/admin-post-destinations.js', import.meta.url);\n\ntest('admin entry loads only active second-round usability helpers', async () => {\n  const source = await readFile(entryPath, 'utf8');\n  assert.match(source, /admin-calendar-settings-usability\\.js/);\n  assert.match(source, /admin-navigation-polish\\.js/);\n  assert.doesNotMatch(source, /admin-manageable-targets\\.js/);\n  assert.doesNotMatch(source, /admin-monitor-labels\\.js/);\n  assert.doesNotMatch(source, /admin-post-destinations\\.js/);\n  assert.doesNotMatch(source, /admin-panel-layout\\.js/);\n  for (const path of [retiredPermissionsPath, retiredLabelsPath, retiredDestinationsPath]) {\n    await assert.rejects(readFile(path, 'utf8'), error => error?.code === 'ENOENT');\n  }\n});\n\ntest('beginner guide keeps calendar first and does not duplicate folded connection settings', async () => {\n  const source = await readFile(guidePath, 'utf8');\n  const calendar = source.indexOf("title: 'カレンダーを見る'");\n  const schedule = source.indexOf("title: '予定・抽選を作る'");\n  assert.ok(calendar >= 0 && schedule > calendar);\n  assert.doesNotMatch(source, /title: 'Googleカレンダー・投稿先を設定する'/);\n});\n\ntest('reaction owner renders manageable channels and read-only rules without a repair pass', async () => {\n  const tools = await readFile(toolsPath, 'utf8');\n  assert.match(tools, /filter\\(channel => channel\\.canManage === true\\)/);\n  assert.match(tools, /channel\\?\\.canManage === true/);\n  assert.match(tools, /reactus-readonly-badge/);\n  assert.match(tools, /閲覧のみ/);\n});\n\ntest('schedule editor owns destination permissions, labels, hydration and refresh', async () => {\n  const source = await readFile(adminPath, 'utf8');\n  assert.match(source, /DESTINATION_TTL_MS = 15_000/);\n  assert.match(source, /monitor\\.canManage !== true/);\n  assert.match(source, /function monitorLabel\\(/);\n  assert.match(source, /duplicateChannelIds/);\n  assert.match(source, /channels=\\$\\{encodeURIComponent\\(mode\\)\\}/);\n  assert.match(source, /const mode = force \\? 'refresh' : '1'/);\n  assert.match(source, /refreshMonitorOptions\\(\\{ silent: true \\}\\)/);\n  assert.match(source, /#monitor'\\)\\.addEventListener\\('focus'/);\n  assert.match(source, /reactus:edit-event/);\n  assert.match(source, /抽選用の投稿先がありません/);\n  assert.match(source, /通常投稿用の投稿先がありません/);\n});\n\ntest('calendar settings owner renders permissions and final wording while pagination follows its lifecycle', async () => {\n  const [owner, usability] = await Promise.all([\n    readFile(calendarSettingsPath, 'utf8'),\n    readFile(settingsPath, 'utf8'),\n  ]);\n  assert.match(owner, /filter\\(item => item\\.canManage === true\\)/);\n  assert.match(owner, /monitor\\.canManage !== true/);\n  assert.match(owner, /reactus-readonly-badge/);\n  assert.match(owner, /friendlyMonitorType/);\n  assert.match(owner, /'抽選' : '通常投稿'/);\n  assert.match(owner, /Googleカレンダーから直接作る予定の合図/);\n  assert.match(owner, /new CustomEvent\\('reactus:calendar-settings-rendered'/);\n  assert.match(usability, /CALENDAR_SETTINGS_PAGE_SIZE = 8/);\n  assert.match(usability, /投稿先・カレンダーで検索/);\n  assert.match(usability, /addEventListener\\('reactus:calendar-settings-rendered'/);\n  assert.doesNotMatch(usability, /new MutationObserver\\(/);\n  assert.doesNotMatch(usability, /\\/api\\/admin\\/bootstrap/);\n});\n\ntest('announcement panel owns its top-level placement without a repair pass', async () => {\n  const source = await readFile(announcementsPath, 'utf8');\n  assert.match(source, /calendar && calendar\\.parentElement === app/);\n  assert.match(source, /calendar\\.after\\(panel\\)/);\n  assert.doesNotMatch(source, /observe\\(document\\.documentElement/);\n});\n\ntest('long dashboards provide a back-to-top control', async () => {\n  const source = await readFile(navigationPath, 'utf8');\n  assert.match(source, /reactusBackToTop/);\n  assert.match(source, /window\\.scrollY < 700/);\n  assert.match(source, /window\\.scrollTo\\(\\{ top: 0, behavior: 'smooth' \\}\\)/);\n});\n`;
await write('tests/adminDashboardPolish.test.mjs', dashboardPolish);

let cacheTest = await read('tests/adminCalendarServerCache.test.mjs');
cacheTest = cacheTest.replace(
  "const monitorLabelsPath = new URL('../public/common/admin-monitor-labels.js', import.meta.url);",
  "const adminUiPath = new URL('../public/admin.js', import.meta.url);",
);
cacheTest = replaceTailFrom(
  cacheTest,
  "test('dashboard hides routing keywords and paginates long reaction-rule lists'",
  `test('dashboard core owns destination labels and reaction rules remain paginated', async () => {\n  const [entry, pagination, adminUi] = await Promise.all([\n    readFile(futureScopePath, 'utf8'),\n    readFile(reactionPaginationPath, 'utf8'),\n    readFile(adminUiPath, 'utf8'),\n  ]);\n  assert.doesNotMatch(entry, /admin-monitor-labels\\.js/);\n  assert.doesNotMatch(entry, /admin-post-destinations\\.js/);\n  assert.match(entry, /admin-reaction-pagination\\.js/);\n  assert.doesNotMatch(entry, /admin-panel-layout\\.js/);\n  assert.match(pagination, /REACTION_PAGE_SIZE = 8/);\n  assert.match(pagination, /reactionRuleSearch/);\n  assert.match(pagination, /← 前へ/);\n  assert.match(pagination, /次へ →/);\n  assert.match(adminUi, /function monitorLabel\\(/);\n  assert.match(adminUi, /monitor\\.canManage !== true/);\n  assert.match(adminUi, /抽選用の投稿先がありません/);\n});\n`,
  'calendar dashboard ownership test',
);
await write('tests/adminCalendarServerCache.test.mjs', cacheTest);

const destinationTest = `import assert from 'node:assert/strict';\nimport { readFile } from 'node:fs/promises';\nimport test from 'node:test';\n\nconst adminPath = new URL('../public/admin.js', import.meta.url);\nconst entryPath = new URL('../public/common/admin-future-scope.js', import.meta.url);\nconst retiredModulePath = new URL('../public/common/admin-post-destinations.js', import.meta.url);\n\ntest('schedule editor owns post destinations instead of a deferred helper', async () => {\n  const [source, entry] = await Promise.all([\n    readFile(adminPath, 'utf8'),\n    readFile(entryPath, 'utf8'),\n  ]);\n\n  assert.doesNotMatch(entry, /admin-post-destinations\\.js/);\n  await assert.rejects(readFile(retiredModulePath, 'utf8'), error => error?.code === 'ENOENT');\n  assert.match(source, /DESTINATION_TTL_MS = 15_000/);\n  assert.match(source, /refreshMonitorOptions/);\n  assert.match(source, /channels=\\$\\{encodeURIComponent\\(mode\\)\\}/);\n  assert.match(source, /const mode = force \\? 'refresh' : '1'/);\n  assert.match(source, /addEventListener\\('focus'/);\n  assert.match(source, /addEventListener\\('change'/);\n  assert.match(source, /reactus:edit-event/);\n  assert.doesNotMatch(source, /new MutationObserver/);\n});\n\ntest('core destination renderer is permission-aware and disambiguates duplicate channels', async () => {\n  const source = await readFile(adminPath, 'utf8');\n  assert.match(source, /monitor\\.canManage !== true/);\n  assert.match(source, /function monitorLabel\\(/);\n  assert.match(source, /duplicateChannelIds/);\n  assert.match(source, /calendarName \\|\\| monitor\\.calendarId/);\n  assert.match(source, /抽選用の投稿先がありません/);\n  assert.match(source, /通常投稿用の投稿先がありません/);\n});\n`;
await write('tests/adminPostDestinationsLazy.test.mjs', destinationTest);

const e2e = `import { expect, test } from '@playwright/test';\n\nconst MANAGEABLE_NORMAL = '3001';\nconst MANAGEABLE_DUPLICATE = '3002';\nconst MANAGEABLE_GIVEAWAY = '3003';\nconst READ_ONLY = '3004';\n\nfunction monitor(id, channelId, channelName, triggerKeyword, canManage, calendarId, calendarName) {\n  return { id, channelId, channelName, triggerKeyword, canManage, calendarId, calendarName };\n}\n\ntest('schedule editor owns destination permissions, labels and refresh lifecycle', async ({ page }) => {\n  let refreshRequests = 0;\n\n  await page.route('**/api/admin/bootstrap**', async route => {\n    const response = await route.fetch();\n    const body = await response.json();\n    const url = new URL(route.request().url());\n    if (url.searchParams.get('channels') === 'refresh') refreshRequests += 1;\n    const monitors = [\n      monitor(MANAGEABLE_NORMAL, '100000000000000101', 'general', '通常', true, 'main@example.com', 'メイン'),\n      monitor(MANAGEABLE_DUPLICATE, '100000000000000101', 'general', '告知', true, 'sub@example.com', 'サブ'),\n      monitor(MANAGEABLE_GIVEAWAY, '100000000000000102', 'giveaway', 'ラキショ', true, 'main@example.com', 'メイン'),\n      monitor(READ_ONLY, '100000000000000103', 'readonly', '通常', false, 'readonly@example.com', '閲覧専用'),\n    ];\n    await route.fulfill({ response, json: { ...body, monitors } });\n  });\n\n  await page.goto('/admin');\n  await expect(page.locator('#monitor')).toBeVisible();\n\n  const normal = page.locator('#monitor option');\n  await expect(normal).toHaveCount(2);\n  await expect(normal.nth(0)).toHaveText('#general (メイン)');\n  await expect(normal.nth(1)).toHaveText('#general (サブ)');\n  expect(await normal.evaluateAll(options => options.map(option => option.value))).not.toContain(READ_ONLY);\n\n  await page.locator('.segment[data-type="giveaway"]').click();\n  await expect(page.locator('#monitor option')).toHaveCount(1);\n  await expect(page.locator('#monitor option')).toHaveText('#giveaway');\n\n  const beforeFocus = refreshRequests;\n  await page.locator('#monitor').focus();\n  await expect.poll(() => refreshRequests).toBeGreaterThan(beforeFocus);\n\n  await page.locator('.segment[data-type="post"]').click();\n  await expect(page.locator('#monitor option')).toHaveCount(2);\n  await page.locator('#monitor').selectOption(MANAGEABLE_DUPLICATE);\n  await page.locator('#monitor').focus();\n  await expect(page.locator('#monitor')).toHaveValue(MANAGEABLE_DUPLICATE);\n});\n`;
await write('tests/e2e/admin-schedule-monitor-owner.spec.mjs', e2e);

console.log('monitor owner refactor applied');
